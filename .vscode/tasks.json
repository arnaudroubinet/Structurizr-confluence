{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Maven: clean compile",
      "type": "shell",
      "command": "mvn",
      "args": ["clean", "compile"],
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },{
      "label": "Maven: clean install",
      "type": "shell",
      "command": "mvn",
      "args": ["clean", "install"],
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },{
      "label": "Maven: test",
      "type": "shell",
      "command": "mvn",
      "args": ["test"],
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
